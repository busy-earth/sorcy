Resolve the latest stable version for tool: $ARGUMENTS

Steps:
1. Run `scripts/resolve-tool-version.sh $ARGUMENTS`
2. Show the resolved version
3. Update the relevant config file to use that version
4. Do not use remembered or guessed versions
5. If resolution fails, stop and explain why
