[package]
name = "sorcy"
version = "0.2.0"
edition = "2021"
description = "CLI for dependency source URL discovery."
license = "MIT"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde_json = "1"
sorcy-core = { path = "../sorcy-core" }
